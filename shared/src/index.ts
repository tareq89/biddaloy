// Shared types, DTOs, and constants between server and clients
// Import from "@beton-boi/shared" in both server and client packages

// Example:
// export interface UserDto {
//   id: string;
//   email: string;
//   name: string;
// }

export {};