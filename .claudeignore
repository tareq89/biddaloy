# Large generated/tracked files Claude shouldn't read directly.
# Query graphify-out/ via `graphify query`, not by reading graph.json raw.
graphify-out/graph.json
graphify-out/cache/

# Regenerated by `yarn docs:generate` — never hand-edit, rarely worth reading in full.
server/openapi.json

# Huge, never useful to read.
yarn.lock
