import { Injectable } from '@nestjs/common';

/** Stub for [17.1.2] — fetches a country/year's public holidays from an
 * external source (`PublicHolidaySource`) into a `PublicHolidaySet`. Filled
 * in by a wave-2 lane. */
@Injectable()
export class PublicHolidayFetchService {}
