import { Injectable } from '@nestjs/common';

/** Stub for [17.1.2] — CRUD for `AcademicTerm`. Filled in by a wave-2 lane,
 * which will record audit entries with `entity_type: 'AcademicTerm'`
 * (already reserved in `AUDIT_ENTITY_TYPES`). */
@Injectable()
export class AcademicTermsService {}
