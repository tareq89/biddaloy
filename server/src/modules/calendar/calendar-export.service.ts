import { Injectable } from '@nestjs/common';

/** Stub for [17.1.2] — exports calendar events (e.g. to a workbook or an
 * ICS feed body). Filled in by a wave-2 lane. */
@Injectable()
export class CalendarExportService {}
