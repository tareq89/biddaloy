import { Injectable } from '@nestjs/common';

/** Stub for [17.1.2] — CRUD for `CalendarEvent` beyond the legacy holiday
 * surface `SchoolCalendarService` already owns. Filled in by a wave-2 lane. */
@Injectable()
export class CalendarEventsService {}
