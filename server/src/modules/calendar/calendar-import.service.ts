import { Injectable } from '@nestjs/common';

/** Stub for [17.1.2] — imports calendar events (e.g. from a workbook or an
 * external feed), producing per-row `CalendarImportRowStatus`. Filled in by
 * a wave-2 lane. */
@Injectable()
export class CalendarImportService {}
