# Epic 8.5 run state

Branch: epic/8.5-e2e-automation (single PR at the end).

## Done

- #126 (8.5.2) — auth/tenant fixtures. Merged via feature/8.5.2-auth-fixtures.
  Deviations (also for PR body):
  - `loggedIn(role)` does a fresh API login per test instead of reusing the
    setup project's storageState files: refresh-token rotation + reuse
    detection (3 grace hops/10s, then family revoked) makes a shared cookie
    file unusable across concurrent tests. Setup project still runs once per
    shard as fail-fast credential validation and writes the files.
  - App fixes required: vite proxy changeOrigin 403'd all silent refreshes
    (SameOriginGuard Origin/Host mismatch); useRouteFocus double race
    (pathname commits before new h1 mounts; data-load re-render replaces
    focused h1); db-reset default-school collision with migration insert.

- #127 (8.5.3) — page objects, i18n helper, route manifest. Merged via
  feature/8.5.3-page-objects.
  Notes: AppShellPage has no logout/user-menu method — the app has no
  user menu yet (no logout UI exists in client-admin). DataTable
  pagination strings are untranslated English literals; page object
  tracks that markup.

## Current

- Next: #127.

## Order

#126 done → #127 done → #312 → #129 → #130 → #128
