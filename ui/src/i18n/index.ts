/**
 * i18next setup, locale registry and per-tenant region configuration.
 *
 * Populated by a later phase-8 task; the barrel exists now so
 * `@beton-boi/ui/i18n` resolves from the moment the package does.
 */
export {};
